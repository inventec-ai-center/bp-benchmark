        apiVersion: v1
        kind: Pod
        metadata:
          labels:
            component: ci
        spec:
          nodeSelector:
            kubernetes.io/hostname: iec1-lugia
          containers:
          - name: ci-runner
            image: 60.250.28.238:30032/pokemon-center/ci_compose_runner
            command:
            - sleep
            args:
            - 99d
            env:
              - name: DOCKER_HOST
                value: tcp://localhost:2375
          - name: docker-daemon
            image: 60.250.28.238:30032/pokemon-center/ci_docker_daemon
            securityContext:
              privileged: true
            env:
              - name: DOCKER_TLS_CERTDIR
                value: ""
